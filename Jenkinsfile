pipeline{
    agent any
    stages{
      stage('start'){
      step{
	  sh '''echo 'starting ....''''
      }
	  }
      stage('build') {
	  step {
	  sh ''' echo 'build...''''
	  }
      
      }
    }
}