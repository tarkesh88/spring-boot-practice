pipeline{
    agent any
    stages{
      stage('start'){
		steps{
			sh '''
				echo 'starting ....'
			'''
		}
	  }
      stage('build') {
		steps {
			sh '''
				echo 'build...'
			'''
		}
      
      }
    }
}